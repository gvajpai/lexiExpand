% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/load_vectors.R
\name{load_vectors}
\alias{load_vectors}
\title{Load cached word vectors into memory}
\usage{
load_vectors(
  model = "glove.6B.50d",
  cache_dir = rappdirs::user_data_dir("lexiExpand"),
  vocab_size = 50000L,
  force_reload = FALSE
)
}
\arguments{
\item{model}{Character scalar. Which cached model to load. Must match
the \code{model} you passed to \code{\link[=download_vectors]{download_vectors()}}. Default \code{"glove.6B.50d"}.}

\item{cache_dir}{Character scalar. Cache directory used by
\code{\link[=download_vectors]{download_vectors()}}. Default: \code{rappdirs::user_data_dir("lexiExpand")}.}

\item{vocab_size}{Integer or \code{NULL}. Maximum number of words to load.
GloVe files are sorted by corpus frequency (most common first), so
loading the top-N words captures the vast majority of real text while
keeping memory use manageable. \code{NULL} loads the full vocabulary
(~400 000 words). Default \code{50000L}.}

\item{force_reload}{Logical. Re-read from disk even if the matrix is
already in the session cache? Useful after updating the cached file.
Default \code{FALSE}.}
}
\value{
A numeric matrix with:
\itemize{
\item \strong{rows} — words (row names set to the vocabulary terms)
\item \strong{columns} — embedding dimensions
\item \strong{values} — L2-normalised so that cosine similarity between two
words equals the dot product of their rows.
}
}
\description{
Reads a cached GloVe \code{.txt} file from disk and returns an L2-normalised
numeric matrix (words × dimensions). The matrix is stored in a
session-level cache so repeated calls within the same R session are
instant.
}
\details{
If the vector file is not found, the function aborts with a clear
message directing the user to run \code{\link[=download_vectors]{download_vectors()}} first.

The session cache is stored in an internal environment
(\code{.lexiexpand_env$vectors}) keyed by \code{"<model>_<vocab_size>"}.
}
\examples{
\dontrun{
download_vectors("glove.6B.50d")

# Load top 50 000 words (default)
vecs <- load_vectors("glove.6B.50d")
dim(vecs)        # 50000 x 50
rownames(vecs)[1:5]  # most frequent words

# Load the full vocabulary
vecs_full <- load_vectors("glove.6B.50d", vocab_size = NULL)
}

}
\seealso{
\code{\link[=download_vectors]{download_vectors()}}, \code{\link[=expand_dict]{expand_dict()}}
}
