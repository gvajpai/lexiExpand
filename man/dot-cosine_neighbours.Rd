% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cosine_sim.R
\name{.cosine_neighbours}
\alias{.cosine_neighbours}
\title{Find cosine nearest neighbours for a set of seed words}
\usage{
.cosine_neighbours(seeds, mat, n = 20L, exclude = seeds)
}
\arguments{
\item{seeds}{Character vector of seed words.}

\item{mat}{Numeric matrix returned by \code{\link[=load_vectors]{load_vectors()}} (L2-normalised rows,
word row names).}

\item{n}{Integer. Number of top candidates to retrieve \strong{per seed word}
before deduplication. Default \code{20L}.}

\item{exclude}{Character vector of words to suppress from results.
Typically the seed words themselves so they are not recommended back.}
}
\value{
A \code{data.frame} with three columns:
\describe{
\item{\code{word}}{Candidate word (character).}
\item{\code{similarity}}{Cosine similarity score in (-1, 1) (numeric).}
\item{\code{seed}}{The seed word that produced this candidate (character).}
}
Rows are sorted descending by \code{similarity}.  Words that appear as
neighbours of multiple seed words are retained under the seed that
yielded the highest similarity.
}
\description{
For each seed word present in \code{mat}, computes the cosine similarity against
every row of \code{mat} (efficiently via matrix multiplication on L2-normalised
vectors), then returns the top-\code{n} candidates across all seeds.
}
\keyword{internal}
