% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/load_vectors.R
\name{load_vectors_textdata}
\alias{load_vectors_textdata}
\title{Load GloVe vectors via the textdata package}
\usage{
load_vectors_textdata(dimensions = 300L, vocab_size = 50000L)
}
\arguments{
\item{dimensions}{Integer. Embedding dimensions to download. One of
\code{50}, \code{100}, \code{200}, or \code{300}. Default \code{300}.}

\item{vocab_size}{Integer or \code{NULL}. Maximum number of words to load.
Default \code{50000L}.}
}
\value{
An L2-normalised numeric matrix (words × dimensions) identical in
structure to the output of \code{\link[=load_vectors]{load_vectors()}}. Compatible with all
\code{\link[=expand_dict]{expand_dict()}} calls via the \code{vectors} argument.
}
\description{
An alternative to \code{\link[=download_vectors]{download_vectors()}} + \code{\link[=load_vectors]{load_vectors()}} that delegates
the download and caching to the \pkg{textdata} package. On first call,
\code{textdata} prompts the user to confirm the download; subsequent calls use
the \code{textdata} cache automatically.
}
\details{
This approach is convenient when \pkg{textdata}
is already installed and you prefer its consent-and-cache flow over the
direct Stanford download in \code{\link[=download_vectors]{download_vectors()}}.
}
\examples{
\dontrun{
# Requires the textdata package
install.packages("textdata")

vecs <- load_vectors_textdata(dimensions = 300, vocab_size = 30000)
result <- expand_dict(c("sad", "angry"), vectors = vecs, interactive = FALSE)
}

}
\seealso{
\code{\link[=load_vectors]{load_vectors()}}, \code{\link[=expand_dict]{expand_dict()}}
}
