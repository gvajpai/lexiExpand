% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cosine_sim.R
\name{.cosine_centroid}
\alias{.cosine_centroid}
\title{Find cosine nearest neighbours using a centroid (averaged) seed vector}
\usage{
.cosine_centroid(seeds, mat, n = 20L, exclude = seeds)
}
\arguments{
\item{seeds}{Character vector of seed words.}

\item{mat}{Numeric matrix returned by \code{\link[=load_vectors]{load_vectors()}} (L2-normalised rows,
word row names).}

\item{n}{Integer. Number of top candidates to retrieve \strong{per seed word}
before deduplication. Default \code{20L}.}

\item{exclude}{Character vector of words to suppress from results.
Typically the seed words themselves so they are not recommended back.}
}
\value{
A \code{data.frame} with columns \code{word}, \code{similarity}, and \code{seed}
(fixed value \code{"centroid"}), sorted descending by \code{similarity}.
}
\description{
Averages all valid seed vectors into a single centroid, L2-normalises it,
then returns the top-\code{n} nearest neighbours from the vocabulary.
}
\details{
This approach finds words near the \emph{semantic centre} of
all seeds jointly — useful when seeds define a coherent concept and you
want words that are collectively close to the whole group rather than close
to any individual seed.
}
\keyword{internal}
