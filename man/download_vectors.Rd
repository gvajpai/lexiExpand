% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/download_vectors.R
\name{download_vectors}
\alias{download_vectors}
\title{Download pre-trained GloVe word vectors}
\usage{
download_vectors(
  model = "glove.6B.50d",
  cache_dir = rappdirs::user_data_dir("lexiExpand"),
  overwrite = FALSE,
  timeout = 3600L
)
}
\arguments{
\item{model}{Character scalar. Which GloVe model to download. One of:
\itemize{
\item \code{"glove.6B.50d"}  (default) — 50 dimensions, ~160 MB extracted
\item \code{"glove.6B.100d"} — 100 dimensions, ~330 MB extracted
\item \code{"glove.6B.200d"} — 200 dimensions, ~660 MB extracted
\item \code{"glove.6B.300d"} — 300 dimensions, ~990 MB extracted
}

All four models share the same source zip (~822 MB). The zip is
downloaded once, the requested file is extracted, and the zip is
deleted to reclaim disk space.}

\item{cache_dir}{Character scalar. Directory in which to cache the
extracted vector file. Defaults to a user-level application data
directory returned by \code{\link[rappdirs:user_data_dir]{rappdirs::user_data_dir()}}.}

\item{overwrite}{Logical. If \code{TRUE}, re-download even when a cached file
exists. Default \code{FALSE}.}

\item{timeout}{Integer. Download timeout in seconds. The default R timeout
(60 s) is far too short for an 822 MB file; this argument temporarily
raises it for the duration of the download and restores the original
value afterwards. Default \code{3600} (one hour), which is comfortable even
on a slow connection. Set lower if you want an earlier failure.}
}
\value{
Invisibly returns the path to the cached \code{.txt} vector file.
Use this path with \code{\link[=load_vectors]{load_vectors()}} or pass it directly to other tools.
}
\description{
Downloads GloVe word vectors from Stanford NLP and caches them on disk.
The download happens \strong{once}; subsequent calls detect the cached file and
return immediately without re-downloading.
}
\examples{
\dontrun{
# Download the compact 50-dimension model (recommended starting point)
download_vectors("glove.6B.50d")

# Explicit timeout (seconds) on a slow connection
download_vectors("glove.6B.50d", timeout = 7200)

# Download a higher-quality model
download_vectors("glove.6B.100d")

# Force re-download
download_vectors("glove.6B.50d", overwrite = TRUE)
}

}
\seealso{
\code{\link[=load_vectors]{load_vectors()}}, \code{\link[=expand_dict]{expand_dict()}}
}
