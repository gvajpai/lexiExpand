% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/review_candidates.R
\name{review_candidates}
\alias{review_candidates}
\title{Interactive candidate review wizard}
\usage{
review_candidates(candidates, seed)
}
\arguments{
\item{candidates}{A \code{data.frame} with at minimum columns \code{word},
\code{similarity}, \code{seed}, and \code{pct_match}, as returned by
\code{\link[=expand_dict]{expand_dict()}} with \code{interactive = FALSE}.}

\item{seed}{Character vector of the original seed words (for display).}
}
\value{
A \code{lexiexpand_result} object (invisibly) with elements:
\describe{
\item{\code{accepted}}{Character vector of words the user accepted.}
\item{\code{seed}}{Original seed words.}
\item{\code{candidates}}{Full candidate data frame (all words, not just accepted).}
}
}
\description{
Presents expansion candidates one by one and asks the researcher whether
to accept each word into the expanded dictionary. Called automatically by
\code{\link[=expand_dict]{expand_dict()}} when \code{interactive = TRUE}, but can also be called directly
on a candidate data frame.
}
\section{Key bindings during the wizard}{

\tabular{ll}{
\code{y} \tab Accept this word \cr
\code{n} \tab Skip this word  \cr
\code{a} \tab Accept this word and \strong{all remaining} candidates \cr
\code{q} \tab Quit; keep words accepted so far \cr
}
}

\examples{
\dontrun{
# Run non-interactively first to get the data frame, then review manually
cands  <- expand_dict(c("sad", "angry"), interactive = FALSE)
result <- review_candidates(cands, seed = c("sad", "angry"))
}

}
\seealso{
\code{\link[=expand_dict]{expand_dict()}}, \code{\link[=export_dict]{export_dict()}}
}
