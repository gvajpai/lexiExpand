% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/expand_dict.R
\name{expand_dict}
\alias{expand_dict}
\title{Expand a seed dictionary using semantic similarity}
\usage{
expand_dict(
  seed,
  n = 20L,
  threshold = 0.65,
  seed_mode = c("individual", "centroid"),
  model = "glove.6B.50d",
  cache_dir = rappdirs::user_data_dir("lexiExpand"),
  vocab_size = 50000L,
  interactive = base::interactive(),
  vectors = NULL
)
}
\arguments{
\item{seed}{Character vector of seed words. Example: \code{c("sad", "angry")}.}

\item{n}{Integer. Number of candidate neighbours to retrieve per seed word
(or per centroid in \code{"centroid"} mode) before applying the threshold.
Default \code{20L}.}

\item{threshold}{Numeric in [0, 1]. Minimum cosine similarity required
to include a candidate. Default \code{0.65}.}

\item{seed_mode}{Character. Multi-seed expansion strategy:
\itemize{
\item \code{"individual"} \emph{(default)} — finds neighbours for each seed word
separately, then deduplicates keeping the highest similarity.
Preserves which seed word drove each match (\verb{$seed} column).
\item \code{"centroid"} — averages all seed vectors into a single centroid and
finds neighbours of that centroid. Returns words nearest the semantic
\emph{centre} of all seeds jointly; better when seeds define a coherent
concept rather than a loose theme. (\verb{$seed} column is \code{"centroid"}.)
}}

\item{model}{Character. Which GloVe model to use. Must already have been
downloaded via \code{\link[=download_vectors]{download_vectors()}}. Default \code{"glove.6B.50d"}.}

\item{cache_dir}{Character. Cache directory. Default:
\code{rappdirs::user_data_dir("lexiExpand")}.}

\item{vocab_size}{Integer or \code{NULL}. Maximum vocabulary loaded by
\code{\link[=load_vectors]{load_vectors()}}. Default \code{50000L}.}

\item{interactive}{Logical. If \code{TRUE} (default when R is interactive),
launches the word-by-word review wizard via \code{\link[=review_candidates]{review_candidates()}}.
If \code{FALSE}, returns the full ranked data frame silently.}

\item{vectors}{Numeric matrix or \code{NULL}. Supply a pre-loaded,
L2-normalised word-vector matrix (as returned by \code{\link[=load_vectors]{load_vectors()}} or
\code{\link[=load_vectors_textdata]{load_vectors_textdata()}}) to skip the loading step.}
}
\value{
\itemize{
\item When \code{interactive = FALSE}: a \code{data.frame} with columns:
\describe{
\item{\code{word}}{Candidate word.}
\item{\code{similarity}}{Cosine similarity to the nearest seed word or
centroid. Ranges from -1 to 1; values below {.arg threshold} are excluded.}
\item{\code{seed}}{Seed word that drove this match, or \code{"centroid"} in
centroid mode.}
\item{\code{pct_match}}{Similarity as a percentage string, e.g. \code{"87\%"}.}
}
\item When \code{interactive = TRUE}: a \code{lexiexpand_result} object (invisibly)
with \verb{$accepted}, \verb{$seed}, and \verb{$candidates}.
}
}
\description{
Given a character vector of seed words, finds semantically similar words
from pre-trained GloVe word embeddings, filters by a similarity threshold,
and either returns the candidate data frame directly or launches an
interactive review wizard.
}
\examples{
\dontrun{
download_vectors("glove.6B.50d")

# Default: per-seed neighbours
candidates <- expand_dict(c("sad", "angry"),
                          n = 15, threshold = 0.70,
                          interactive = FALSE)

# Centroid mode: find words near the semantic centre of both seeds
candidates_c <- expand_dict(c("sad", "angry"),
                            seed_mode   = "centroid",
                            n           = 15,
                            threshold   = 0.70,
                            interactive = FALSE)

# Using textdata as the vector source
vecs <- load_vectors_textdata(dimensions = 300, vocab_size = 30000)
result <- expand_dict(c("innovation", "invention"),
                      vectors = vecs, interactive = FALSE)
}

}
\seealso{
\code{\link[=download_vectors]{download_vectors()}}, \code{\link[=load_vectors]{load_vectors()}}, \code{\link[=load_vectors_textdata]{load_vectors_textdata()}},
\code{\link[=review_candidates]{review_candidates()}}, \code{\link[=export_dict]{export_dict()}}
}
