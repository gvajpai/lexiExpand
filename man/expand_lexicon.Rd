% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/expand_dict.R
\name{expand_lexicon}
\alias{expand_lexicon}
\title{Expand a lexicon-style data frame by dimension}
\usage{
expand_lexicon(
  lexicon,
  word_col = NULL,
  dim_col = NULL,
  n = 20L,
  threshold = 0.65,
  seed_mode = c("individual", "centroid"),
  vectors = NULL,
  model = "glove.6B.50d",
  cache_dir = rappdirs::user_data_dir("lexiExpand"),
  vocab_size = 50000L
)
}
\arguments{
\item{lexicon}{A data frame with at least two columns. The first column
is treated as words; the second as dimension labels. Column names are
detected automatically, or you can specify them explicitly via
\code{word_col} and \code{dim_col}.}

\item{word_col}{Character. Name of the word column. If \code{NULL} (default),
the first column is used.}

\item{dim_col}{Character. Name of the dimension column. If \code{NULL}
(default), the second column is used.}

\item{n}{Integer. Candidates per seed word per dimension. Default \code{20L}.}

\item{threshold}{Numeric. Minimum cosine similarity. Default \code{0.65}.}

\item{seed_mode}{Character. \code{"individual"} (default) or \code{"centroid"}.
See \code{\link[=expand_dict]{expand_dict()}}.}

\item{vectors}{Numeric matrix or \code{NULL}. Pre-loaded vector matrix from
\code{\link[=load_vectors]{load_vectors()}}. Strongly recommended when calling this function to
avoid reloading vectors for each dimension.}

\item{model, cache_dir, vocab_size}{Passed to \code{\link[=load_vectors]{load_vectors()}} if \code{vectors}
is \code{NULL}.}
}
\value{
A data frame with columns \code{word}, \code{similarity}, \code{seed},
\code{pct_match}, and \code{dimension} — one row per candidate across all
dimensions, sorted by dimension then descending similarity. Words
already present in the lexicon are excluded from results.
}
\description{
Accepts a two-column data frame where the first column contains words and
the second column contains their dimension labels (e.g. the \code{mde_words}
object from \pkg{mdeinR}). Runs \code{\link[=expand_dict]{expand_dict()}} on each unique dimension
group and returns all candidate expansions in a single tidy data frame.
}
\examples{
\dontrun{
# Typical use with mdeinR
library(mdeinR)
vecs <- load_vectors("glove.6B.50d")

# mde_words is a data frame: columns 'word' and 'dimension'
candidates <- expand_lexicon(mde_words, vectors = vecs)
head(candidates)

# Works with any two-column data frame
my_lexicon <- data.frame(
  term  = c("delicious", "fragrant", "joyful", "warm"),
  group = c("sensory",   "sensory",  "affect", "affect")
)
expand_lexicon(my_lexicon, vectors = vecs, threshold = 0.70)
}

}
\seealso{
\code{\link[=expand_dict]{expand_dict()}}, \code{\link[=export_dict]{export_dict()}}
}
