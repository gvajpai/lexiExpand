% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/export_dict.R
\name{export_dict}
\alias{export_dict}
\title{Export an expanded dictionary to analysis-ready formats}
\usage{
export_dict(
  result,
  seed = NULL,
  name = "category",
  format = "list",
  include_seed = TRUE
)
}
\arguments{
\item{result}{One of:
\itemize{
\item A \code{lexiexpand_result} object returned by \code{\link[=expand_dict]{expand_dict()}} /
\code{\link[=review_candidates]{review_candidates()}} (interactive workflow), \strong{or}
\item A \code{data.frame} returned by \code{\link[=expand_dict]{expand_dict()}} with
\code{interactive = FALSE} — the \verb{$word} column is used automatically, \strong{or}
\item A plain character vector of accepted words (in which case \code{seed}
must also be supplied).
}}

\item{seed}{Character vector of seed words. Ignored when \code{result} is an
\code{lexiexpand_result}; required when \code{result} is a character vector.}

\item{name}{Character scalar. Category name for the dictionary entry.
Default \code{"category"}. Example: \code{"negative_emotion"}, \code{"innovation"}.}

\item{format}{Character scalar. Output format. One of:
\describe{
\item{\code{"list"}}{(Default) A named list, e.g.
\code{list(negative_emotion = c("sad", "angry", "miserable", ...))}.
Drop-in compatible with \pkg{quanteda} \code{dictionary()} and
\pkg{tidytext} \code{match_any()}.}
\item{\code{"data.frame"}}{A two-column \code{data.frame} with columns \code{word}
and \code{category}. Easy to write to CSV or join with other data.}
\item{\code{"quanteda"}}{A \code{quanteda::dictionary} object. Requires the
\pkg{quanteda} package.}
}}

\item{include_seed}{Logical. Include the original seed words in the
exported dictionary? Default \code{TRUE}.}
}
\value{
Depends on \code{format}:
\itemize{
\item \code{"list"}: named \code{list}.
\item \code{"data.frame"}: \code{data.frame}.
\item \code{"quanteda"}: \code{quanteda::dictionary}.
}
}
\description{
Converts the output of \code{\link[=review_candidates]{review_candidates()}} (or a plain character vector
of words) into a format ready for use with \pkg{quanteda}, \pkg{tidytext},
or any other text-analysis workflow.
}
\examples{
\dontrun{
download_vectors("glove.6B.50d")

# Fully scripted pipeline (no interactive wizard)
result <- expand_dict(c("sad", "angry"), threshold = 0.70,
                      interactive = FALSE)

# Export top candidates automatically (e.g. top 10 by similarity)
top_words <- head(result$word, 10)

export_dict(top_words,
            seed   = c("sad", "angry"),
            name   = "negative_emotion",
            format = "list")

# From an interactive result object
result <- expand_dict(c("sad", "angry"))    # launches wizard
export_dict(result, name = "negative_emotion", format = "quanteda")
}

}
\seealso{
\code{\link[=expand_dict]{expand_dict()}}, \code{\link[=review_candidates]{review_candidates()}}
}
